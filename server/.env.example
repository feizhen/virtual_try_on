# Application Configuration
NODE_ENV=development
PORT=3000
APP_NAME="Vibe Coding Template"
APP_URL=http://localhost:3000

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/vibe_db?schema=public"

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production-min-32-chars
JWT_REFRESH_EXPIRES_IN=30d

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Railway Configuration (automatically injected by Railway)
# RAILWAY_ENVIRONMENT=production
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# Google Gemini API
GEMINI_API_KEY=sk-PRV0HXW9ONOTIXDn52FdEf0fA02f466386F960C9C67eF576
GEMINI_API_BASE_URL=https://api.laozhang.ai
GEMINI_TIMEOUT=300  # API 超时时间（秒），建议 300 (5分钟) 以适应图像生成耗时

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# File Upload Configuration
UPLOAD_DIR=uploads

# Storage Configuration
STORAGE_TYPE=local  # 'local' or 'tos'

# TOS (Volcengine Object Storage) Configuration
# 仅当 STORAGE_TYPE=tos 时需要配置以下项
TOS_ACCESS_KEY_ID=your_tos_access_key_id_here
TOS_SECRET_ACCESS_KEY=your_tos_secret_access_key_here
TOS_REGION=cn-beijing
TOS_ENDPOINT=tos-cn-beijing.volces.com
TOS_BUCKET=virtual-try-on-dev
TOS_CDN_DOMAIN=https://cdn-dev.virtual-try-on.com
# TOS_URL_EXPIRES_IN: 预签名 URL 过期时间（秒），默认 86400 (24小时)
# 注意：使用预签名 URL 方案后，不需要设置 Bucket 公网读权限
TOS_URL_EXPIRES_IN=86400
