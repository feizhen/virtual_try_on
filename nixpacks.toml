[phases.setup]
nixPkgs = ['nodejs_20', 'pnpm']

[phases.install]
cmds = [
  'pnpm install --frozen-lockfile',
  'npx prisma generate'
]

[phases.build]
cmds = ['pnpm run build']

[start]
cmd = 'npx prisma migrate deploy && pnpm run start:prod'
